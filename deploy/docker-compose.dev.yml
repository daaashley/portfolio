version: '3.9'

services:
  api:
    ports:
      # Exposes application port.
    - "5050:5050"
    volumes:
      # Adds current directory as volume.
    - .:/app/src/
    environment:
      # Enables autoreload.
      BACKEND_RELOAD: "True"
